target/
.target
.vscode/
.cargo/
.claude/

/.idea/.gitignore
/.idea/modules.xml
/.idea/moor.iml
/.idea/vcs.xml
/.idea

*.pem
.aider*
perf.data

mise.toml

development-export/
export/
node_modules/
dist/

# Claude artifacts
CLAUDE.md
CLAUDE.sessions.md
sessions/
/.claude/

# Runtime data and IPC (managed by start scripts)
run-cowbell/
run-lambda-moor/

# OAuth2 config files with secrets
*-oauth2.yaml
my-*.yaml
web-host-production.yaml

# Docker Compose environment-specific overrides
docker-compose.override.yml

# Production deployment directory (contains secrets, configs, scripts)
prod/

# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# LambdaMOO sources (fetched by setup script for comparative testing)
lambdamoo/
certs/

# macOS
.DS_Store

# Cowbell core (managed by cores/fetch-cowbell.sh)
cores/cowbell/

# Meadow web client (managed by clients/fetch-meadow.sh)
clients/meadow/
clients/moor-web-mcp/*.local.yaml
clients/moor-web-mcp/*.local.json

# Proptest failure snapshots (regenerated on failure)
proptest-failures/
.mcp.json
.mcp.json
