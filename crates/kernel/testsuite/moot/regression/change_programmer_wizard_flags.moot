; $object = create(#2);

; return {$object.programmer, $object.wizard};
{0, 0}

; $object.programmer = 1;
; return {$object.programmer, $object.wizard};
{1, 0}

; $object.wizard = 1;
; return {$object.programmer, $object.wizard};
{1, 1}

; $object.programmer = 0;
; return {$object.programmer, $object.wizard};
{0, 1}

; $object.wizard = 0;
; return {$object.programmer, $object.wizard};
{0, 0}
