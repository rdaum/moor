// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from "flatbuffers";

import { PropDef } from "../moor-common/prop-def.js";

export class PropDefs {
    bb: flatbuffers.ByteBuffer | null = null;
    bb_pos = 0;
    __init(i: number, bb: flatbuffers.ByteBuffer): PropDefs {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }

    static getRootAsPropDefs(bb: flatbuffers.ByteBuffer, obj?: PropDefs): PropDefs {
        return (obj || new PropDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }

    static getSizePrefixedRootAsPropDefs(bb: flatbuffers.ByteBuffer, obj?: PropDefs): PropDefs {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PropDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }

    props(index: number, obj?: PropDef): PropDef | null {
        const offset = this.bb!.__offset(this.bb_pos, 4);
        return offset
            ? (obj || new PropDef()).__init(
                this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4),
                this.bb!,
            )
            : null;
    }

    propsLength(): number {
        const offset = this.bb!.__offset(this.bb_pos, 4);
        return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
    }

    static startPropDefs(builder: flatbuffers.Builder) {
        builder.startObject(1);
    }

    static addProps(builder: flatbuffers.Builder, propsOffset: flatbuffers.Offset) {
        builder.addFieldOffset(0, propsOffset, 0);
    }

    static createPropsVector(builder: flatbuffers.Builder, data: flatbuffers.Offset[]): flatbuffers.Offset {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]!);
        }
        return builder.endVector();
    }

    static startPropsVector(builder: flatbuffers.Builder, numElems: number) {
        builder.startVector(4, numElems, 4);
    }

    static endPropDefs(builder: flatbuffers.Builder): flatbuffers.Offset {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // props
        return offset;
    }

    static createPropDefs(builder: flatbuffers.Builder, propsOffset: flatbuffers.Offset): flatbuffers.Offset {
        PropDefs.startPropDefs(builder);
        PropDefs.addProps(builder, propsOffset);
        return PropDefs.endPropDefs(builder);
    }
}
