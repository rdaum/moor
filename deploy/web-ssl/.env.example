# Web-SSL Deployment Configuration (HTTPS with Let's Encrypt)
# Copy this file to .env and customize for your deployment

# REQUIRED: Your domain name (must point to this server's public IP)
DOMAIN_NAME=your-domain.com

# REQUIRED: Email for Let's Encrypt notifications (certificate expiration, etc.)
CERTBOT_EMAIL=your-email@example.com

# Telnet server port (default: 8888)
TELNET_PORT=8888

# Database configuration
DATABASE_NAME=production.db

# Build profile (release for production, debug for testing)
BUILD_PROFILE=release

# Rust backtrace level for debugging (0, 1, or full)
RUST_BACKTRACE=1

# User and group IDs for running containers (optional, defaults to root)
# For better security, set these to your user's UID/GID:
#   export USER_ID=$(id -u) GROUP_ID=$(id -g)
# Or uncomment and set manually:
# USER_ID=1000
# GROUP_ID=1000
