// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from "flatbuffers";

import { VerbDef } from "../moor-common/verb-def";

export class VerbDefs {
    bb: flatbuffers.ByteBuffer | null = null;
    bb_pos = 0;
    __init(i: number, bb: flatbuffers.ByteBuffer): VerbDefs {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }

    static getRootAsVerbDefs(bb: flatbuffers.ByteBuffer, obj?: VerbDefs): VerbDefs {
        return (obj || new VerbDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }

    static getSizePrefixedRootAsVerbDefs(bb: flatbuffers.ByteBuffer, obj?: VerbDefs): VerbDefs {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new VerbDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }

    verbs(index: number, obj?: VerbDef): VerbDef | null {
        const offset = this.bb!.__offset(this.bb_pos, 4);
        return offset
            ? (obj || new VerbDef()).__init(
                this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4),
                this.bb!,
            )
            : null;
    }

    verbsLength(): number {
        const offset = this.bb!.__offset(this.bb_pos, 4);
        return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
    }

    static startVerbDefs(builder: flatbuffers.Builder) {
        builder.startObject(1);
    }

    static addVerbs(builder: flatbuffers.Builder, verbsOffset: flatbuffers.Offset) {
        builder.addFieldOffset(0, verbsOffset, 0);
    }

    static createVerbsVector(builder: flatbuffers.Builder, data: flatbuffers.Offset[]): flatbuffers.Offset {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]!);
        }
        return builder.endVector();
    }

    static startVerbsVector(builder: flatbuffers.Builder, numElems: number) {
        builder.startVector(4, numElems, 4);
    }

    static endVerbDefs(builder: flatbuffers.Builder): flatbuffers.Offset {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // verbs
        return offset;
    }

    static createVerbDefs(builder: flatbuffers.Builder, verbsOffset: flatbuffers.Offset): flatbuffers.Offset {
        VerbDefs.startVerbDefs(builder);
        VerbDefs.addVerbs(builder, verbsOffset);
        return VerbDefs.endVerbDefs(builder);
    }
}
