// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from "flatbuffers";

import { StoredNameDeclPair } from "../moor-program/stored-name-decl-pair";

export class StoredNames {
    bb: flatbuffers.ByteBuffer | null = null;
    bb_pos = 0;
    __init(i: number, bb: flatbuffers.ByteBuffer): StoredNames {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }

    static getRootAsStoredNames(bb: flatbuffers.ByteBuffer, obj?: StoredNames): StoredNames {
        return (obj || new StoredNames()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }

    static getSizePrefixedRootAsStoredNames(bb: flatbuffers.ByteBuffer, obj?: StoredNames): StoredNames {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new StoredNames()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }

    globalWidth(): bigint {
        const offset = this.bb!.__offset(this.bb_pos, 4);
        return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt("0");
    }

    decls(index: number, obj?: StoredNameDeclPair): StoredNameDeclPair | null {
        const offset = this.bb!.__offset(this.bb_pos, 6);
        return offset
            ? (obj || new StoredNameDeclPair()).__init(
                this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4),
                this.bb!,
            )
            : null;
    }

    declsLength(): number {
        const offset = this.bb!.__offset(this.bb_pos, 6);
        return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
    }

    static startStoredNames(builder: flatbuffers.Builder) {
        builder.startObject(2);
    }

    static addGlobalWidth(builder: flatbuffers.Builder, globalWidth: bigint) {
        builder.addFieldInt64(0, globalWidth, BigInt("0"));
    }

    static addDecls(builder: flatbuffers.Builder, declsOffset: flatbuffers.Offset) {
        builder.addFieldOffset(1, declsOffset, 0);
    }

    static createDeclsVector(builder: flatbuffers.Builder, data: flatbuffers.Offset[]): flatbuffers.Offset {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]!);
        }
        return builder.endVector();
    }

    static startDeclsVector(builder: flatbuffers.Builder, numElems: number) {
        builder.startVector(4, numElems, 4);
    }

    static endStoredNames(builder: flatbuffers.Builder): flatbuffers.Offset {
        const offset = builder.endObject();
        builder.requiredField(offset, 6); // decls
        return offset;
    }

    static createStoredNames(
        builder: flatbuffers.Builder,
        globalWidth: bigint,
        declsOffset: flatbuffers.Offset,
    ): flatbuffers.Offset {
        StoredNames.startStoredNames(builder);
        StoredNames.addGlobalWidth(builder, globalWidth);
        StoredNames.addDecls(builder, declsOffset);
        return StoredNames.endStoredNames(builder);
    }
}
