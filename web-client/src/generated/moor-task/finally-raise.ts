// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from "flatbuffers";

import { Exception } from "../moor-common/exception";

export class FinallyRaise {
    bb: flatbuffers.ByteBuffer | null = null;
    bb_pos = 0;
    __init(i: number, bb: flatbuffers.ByteBuffer): FinallyRaise {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }

    static getRootAsFinallyRaise(bb: flatbuffers.ByteBuffer, obj?: FinallyRaise): FinallyRaise {
        return (obj || new FinallyRaise()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }

    static getSizePrefixedRootAsFinallyRaise(bb: flatbuffers.ByteBuffer, obj?: FinallyRaise): FinallyRaise {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new FinallyRaise()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }

    exception(obj?: Exception): Exception | null {
        const offset = this.bb!.__offset(this.bb_pos, 4);
        return offset ? (obj || new Exception()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
    }

    static startFinallyRaise(builder: flatbuffers.Builder) {
        builder.startObject(1);
    }

    static addException(builder: flatbuffers.Builder, exceptionOffset: flatbuffers.Offset) {
        builder.addFieldOffset(0, exceptionOffset, 0);
    }

    static endFinallyRaise(builder: flatbuffers.Builder): flatbuffers.Offset {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // exception
        return offset;
    }

    static createFinallyRaise(builder: flatbuffers.Builder, exceptionOffset: flatbuffers.Offset): flatbuffers.Offset {
        FinallyRaise.startFinallyRaise(builder);
        FinallyRaise.addException(builder, exceptionOffset);
        return FinallyRaise.endFinallyRaise(builder);
    }
}
